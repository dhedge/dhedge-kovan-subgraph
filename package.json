{
  "name": "dhedge-kovan",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ hedgedon/dhedge-kovan",
    "create-local": "graph create --node http://localhost:8020/ hedgedon/dhedge-kovan",
    "remove-local": "graph remove --node http://localhost:8020/ hedgedon/dhedge-kovan",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hedgedon/dhedge-kovan"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
